import { Unless.DirectiveDirective } from './unless.directive.directive';

describe('Unless.DirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new Unless.DirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
